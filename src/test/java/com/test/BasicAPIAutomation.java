package com.test;

import java.util.HashMap;
import java.util.Map;

import org.json.simple.JSONObject;

public class BasicAPIAutomation {

	/*
	  Command+Shift+o - it will remove all the uncessary imported files and will organize in very well.
	  Json path finder - https://jsonpathfinder.com/
	  
	  if you want to format pom.xml file the use these commands : command+a after than use command+shift+f
	  
	  
	  for sending the data using Map and JSON object
	  Map<String, String> map= new HashMap<>();
		
		map.put("name", "Sukhdev");
		map.put("job", "Test Lead");
		
		System.out.println(map);
		
		JSONObject jsonobj= new JSONObject(map);
		 System.out.println(jsonobj.toJSONString());
		 
		 
		 
		 
	  
	   
	 
	 
	 
	 
	 */
	
	
	
}
